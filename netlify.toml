# Netlify deployment settings - Force rebuild v1.1
[build]
  command = "npm ci && npm run build"
  publish = "dist"

# Redirect all routes to index.html for single-page app
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200